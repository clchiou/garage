---

# Path to the this shipyard.
shipyard: "{{ playbook_dir }}"

# Directory for checking out source code repos.
build_src: "{{ lookup('env', 'HOME') }}/build"

# Working directory for building images (later you archive from here).
build_out: "{{ lookup('env', 'HOME') }}/rootfs"
