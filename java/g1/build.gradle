//
// Build script for configuring the projects.
//

plugins {
    id 'com.github.johnrengelman.shadow' version '7.1.0'
    id 'java'
    id 'java-library'
    id 'nu.studer.jooq' version '6.0.1'
}

// For now the root project is empty.
shadowJar.enabled = false
