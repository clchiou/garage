//
// Enumerate sub-projects
//
// And be flat - while gradle allows deep sub-projects, it seems to
// prefer a flattened structure.
//

ext {
    garageProjects = [
        'base',
        'search-analyzer',
        'search-indexer',
        'search-server',
    ]
}
